#!/bin/bash

# run `source project_name_env.sh` to set the env variable

# This file is used to simplify setting the following
# environment variable. You may want to edit its value
# here to a value describing your project.

# The env variable `COMPOSE_PROJECT_NAME` is used
# by *docker-compose* to prefix the container names it creates.

export COMPOSE_PROJECT_NAME=phpwebtemplate
